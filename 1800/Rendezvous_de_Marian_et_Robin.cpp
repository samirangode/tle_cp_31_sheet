#include <bits/stdc++.h>

using namespace std;


void work(){
    int n, m, h;

    cin>>n;
    cin>>m;
    cin>>h;

}

int main(){
    ios::sync_with_stdio(0);
    cin.tie(nullptr);

    int t;
    cin>>t;
    while(t--){
        work();
    }
}
